import { Layout } from "antd";
import React from "react";

export default function Footer() {
    return (
        <Layout.Footer style={{ background: "#0d5b10", height: "80px" }}>
            {/* <div className="Footer-left-menu">
                {width < 768 ? (
                    <div className="menu-left-icon menu-left-icon-menu-collapse-on-close"></div>
                ) : null}
            </div> */}
        </Layout.Footer>
    );
}
